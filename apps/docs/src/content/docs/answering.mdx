---
title: Answering
description: The answer node. Passages as top-level search_result blocks, citations streaming as they arrive, and no tools key in the request at all.
order: 13
group: The Support Graph
---

`answer` runs when either level's route says the evidence holds. It sends that level's passages and the question to the model with citations enabled, and streams the reply. `answer_request_kwargs()` in `agent/graph.py` assembles the request.

The interesting part of that request is what is missing from it.

---

## The flow

```
answer(state)
  │
  ├─ answer_request_kwargs()
  │     search_result blocks (passages first) + the question (last)
  │     system prompt, cached · history · no `tools` key
  ▼
Messages API (streaming, citations enabled)
  │  text_delta      ──▶ stream writer ──▶ SSE `text`
  │  citations_delta ──▶ stream writer ──▶ SSE `citation`, as it arrives
  ▼
_collect_segments(response)             shared with loop.py
  └─▶ state: answer · citations · segments · outcome="answer"
```

There is no `tools` key in the request. Not an empty list: the key never appears, and neither does the ticket tool's name. A test asserts both on the real request.

`_collect_segments()` is the loop engine's own function, so the renderer and the stored format do not depend on which engine ran.

---

## A worked example

**"My Cloud Run container fails to start — what does the PORT error mean?"** A real run.

- Level 1 passed it on similarity `0.62` and groundedness `0.95`, with five passages.
- The request goes out as five `search_result` blocks followed by the question, plus a cached system prompt. No tools.
- The reply streams as `text` events, and a `citation` event each time the model attributes a claim. Two arrived.
- The step records `level: 1`, `n_citations: 2`, and the guardrail's sentence as its reasoning. The node took 2.5 s; the whole turn cost $0.026.

The follow-up in the same conversation reached this node from Level 2. Same request shape, built from the Level 2 passages instead, and a step with `level: 2` and three citations.

---

## Design decisions

### The model has nothing to escalate with

Escalation is not this node's decision, and the enforcement is structural rather than written down.

| | **No `tools` key (built)** | Tools present, prompt says "don't escalate" |
|---|---|---|
| **Can it file a ticket?** | No, the capability is absent | Yes, if it decides to |
| **Can it decline by escalating?** | No | Yes, the easy out on any hard question |
| **What enforces it** | The API request | A sentence the model weighs against others |
| **How you verify it** | Assert on the request in a test | Re-run the evals and hope |

Prompting a model not to do something it can do is a request. Removing the capability is a guarantee.

### The request itself

- **`search_result` blocks go in as top-level user content.** They do not have to arrive inside a `tool_result`, so retrieval can be a node while citations work exactly as they do in the loop.
- **Passages first, question last.** The evidence is context; the question is the instruction.
- **Citations are enabled on every passage.** The API requires them on all search results in a request or none.
- **The system prompt is cached.** One `cache_control` breakpoint on a block that does not change between turns. See [Prompt Caching](/configent/docs/prompt-caching).
- **Deltas go through LangGraph's stream writer.** The node writes `("text", …)` and `("citation", …)` tuples, and `stream_graph()` forwards them unchanged. See [SSE Streaming](/configent/docs/sse-streaming#how-graph-nodes-stream).
- **The response is assembled, not generated.** Citations and `output_config.format` cannot be combined, so Python builds the final response from state.
- **This node never sees empty retrieval.** Both decide functions require at least one passage before they route here.
- **An answer that cites nothing is visible, not blocked.** `n_citations: 0` on the step reads as a corpus problem rather than a wiring one.

---

## Configuration

| Key | Default | What it does |
|---|---|---|
| `agent.model` | none | The model that answers. The same one grades |
| `agent.max_tokens` | `4096` | Cap on the streamed reply |
| `agent.system_prompt_file` | none | The answer prompt. `grade.md`, `rewrite.md` and `ticket_draft.md` are resolved beside it |

Citation payloads and how the frontend renders them: [Citations](/configent/docs/citations).

---

**Next:** [Escalation & Ticket](/configent/docs/escalation-ticket), for the turn neither level could answer.
